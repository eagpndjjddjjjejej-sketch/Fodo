name: vps

# Controls when the workflow will run
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run VPS
        run: curl -sSf https://sshx.io/get | sh -s run        } else {
            Write-Host "Folders found in extracted:"
            $folders | ForEach-Object { Write-Host $_.Name }
            $global:PROJECT_FOLDER = $folders[0].FullName
        }
        Write-Host "Using project folder: $global:PROJECT_FOLDER"

    # 4️⃣ Setup .NET SDK
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.0.x'

    # 5️⃣ Install Android workload
    - name: Install Android Workload
      run: dotnet workload install android

    # 6️⃣ Restore & Build project
    - name: Restore and Build
      shell: powershell
      run: |
        cd $env:PROJECT_FOLDER
        dotnet restore
        dotnet build -c Release

    # 7️⃣ Upload APK artifact
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Chaincase-APK
        path: $env:PROJECT_FOLDER\bin\Release\*.apk            Write-Host "Folders found in extracted:"
            $folders | ForEach-Object { Write-Host $_.Name }
            $global:PROJECT_FOLDER = $folders[0].FullName
        }
        Write-Host "Using project folder: $global:PROJECT_FOLDER"

    # 4️⃣ Setup .NET SDK
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.0.x'

    # 5️⃣ Install Android workload
    - name: Install Android Workload
      run: dotnet workload install android

    # 6️⃣ Restore & Build project
    - name: Restore and Build
      shell: powershell
      run: |
        cd $env:PROJECT_FOLDER
        dotnet restore
        dotnet build -c Release

    # 7️⃣ Upload APK artifact
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Chaincase-APK
        path: $env:PROJECT_FOLDER\bin\Release\*.apk        $zip.Dispose()

    # 4️⃣ Unzip project
    - name: Unzip project
      shell: powershell
      run: Expand-Archive -Path project.zip -DestinationPath extracted -Force

    # 5️⃣ Detect main project folder automatically
    - name: Detect main project folder
      shell: powershell
      run: |
        $folders = Get-ChildItem -Path extracted -Directory
        if ($folders.Count -eq 0) {
            Write-Host "No subfolders found, using 'extracted' as project folder"
            $global:PROJECT_FOLDER = "extracted"
        } else {
            Write-Host "Folders found in extracted:"
            $folders | ForEach-Object { Write-Host $_.Name }
            $global:PROJECT_FOLDER = $folders[0].FullName
        }
        Write-Host "Using project folder: $global:PROJECT_FOLDER"

    # 6️⃣ Setup .NET SDK
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.0.x'

    # 7️⃣ Install Android workload
    - name: Install Android Workload
      run: dotnet workload install android

    # 8️⃣ Restore & Build project
    - name: Restore and Build
      shell: powershell
      run: |
        cd $env:PROJECT_FOLDER
        dotnet restore
        dotnet build -c Release

    # 9️⃣ Upload APK artifact
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Chaincase-APK
        path: $env:PROJECT_FOLDER\bin\Release\*.apk
